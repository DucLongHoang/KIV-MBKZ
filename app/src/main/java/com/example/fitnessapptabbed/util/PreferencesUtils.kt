package com.example.fitnessapptabbed.util

/**
 * [PreferencesUtils] class that holds some static constants
 * @author Long
 * @version 1.0
 */
class PreferencesUtils {
    companion object {
        const val KEY_NAME = "TRAIN_NAME"
        const val KEY_DATE = "TRAIN_DATE"
        const val KEY_DURATION = "TRAIN_DUR"
        const val KEY_MODE = "MODE"
    }
}